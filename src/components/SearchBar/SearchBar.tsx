import { QualityList } from './QualityList';
import { SearchInput } from './SearchInput';

export const SearchBar = () => {
  return (
    <>
      <SearchInput />
      <QualityList />
    </>
  );
};
